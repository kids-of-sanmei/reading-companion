# Day 6 Notes - Embeddings

## 观察记录

- 向量维度：
- 输出记录里的 metadata：
- 离线 hash embedding 与真实 API embedding 的区别：

## 问题

- 为什么不能只保存 embedding，不保存原始 `text` 和 `source`？
- 为什么同一个 chunk 的 `chunk_id` 要稳定？
- 下一步做 vector search 时，需要比较什么？
