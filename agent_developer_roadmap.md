# Agent 开发工程师学习计划

目标：从零基础逐步成长为合格的 Agent 开发工程师，能够独立完成一个可运行、可维护、可评估的 AI Agent 应用。

## 总体路线

不要一开始就追求复杂框架和高级概念。正确顺序是：

1. 先掌握 Python 和 Web/API 基础。
2. 再理解 OpenAI API 的底层调用方式。
3. 再学习 RAG、工具调用、记忆、工作流。
4. 再使用 LangChain、LangGraph 等框架提高工程效率。
5. 最后通过项目、评估、部署和排错形成工程能力。

建议周期：3 到 6 个月。每天 1 到 2 小时，周末做项目整合。

## 第一阶段：编程和工程基础

目标：能看懂并写出一个结构清晰的 Python 小项目。

需要掌握：

- Python 基础语法：变量、函数、类、异常、文件读写。
- 常用数据结构：list、dict、set、tuple。
- 虚拟环境和依赖管理：`venv`、`pip`、`requirements.txt`。
- Git 基础：clone、commit、branch、diff、ignore。
- HTTP 基础：请求、响应、JSON、状态码。
- 命令行基础：运行脚本、传参、查看错误日志。

练习项目：

- 写一个命令行待办事项工具。
- 写一个读取 Markdown 文件并生成摘要的小工具。
- 写一个调用公开 API 并保存结果为 JSON 的脚本。

验收标准：

- 能独立创建 Python 项目。
- 能定位常见报错，如导入失败、路径错误、依赖缺失。
- 能用 Git 保存自己的代码版本。

## 第二阶段：OpenAI API 基础

目标：理解大模型应用的底层调用方式，而不是只会套框架。

需要掌握：

- Chat Completions / Responses API 的基本调用方式。
- system、user、assistant 消息的区别。
- prompt 的结构设计。
- 多轮对话历史如何传入模型。
- temperature、model、max tokens 等参数的作用。
- 流式输出的基本概念。
- API key、base url、环境变量配置。

练习项目：

- 写一个最简单的命令行聊天机器人。
- 给聊天机器人增加 system prompt。
- 给聊天机器人增加多轮历史。
- 把对话记录保存到本地 JSONL 文件。

验收标准：

- 不依赖 LangChain，也能直接调用 OpenAI API 完成聊天。
- 能解释一次模型调用从输入到输出发生了什么。
- 能看懂 API 返回结构并提取模型回答。

## 第三阶段：RAG 检索增强生成

目标：能做一个“基于资料回答问题”的 Agent 基础能力。

需要掌握：

- 文档读取：TXT、Markdown、PDF。
- 文本切片：chunk size、overlap、分隔符。
- Embedding 的作用。
- 向量数据库的基本概念。
- 相似度检索和 top-k。
- 把检索结果拼进 prompt。
- 回答时要求模型基于资料，不乱编。

推荐工具：

- 本地轻量方案：Chroma、FAISS。
- 工程化方案：Milvus、Postgres + pgvector。
- 文档解析：pypdf、unstructured。

练习项目：

- 做一个“问自己的笔记”的问答工具。
- 做一个“问 PDF”的命令行工具。
- 给回答附上引用片段和来源文件。

验收标准：

- 能解释 RAG 的完整流程：读取、切片、embedding、入库、检索、生成。
- 能调 chunk size 和 top-k 改善效果。
- 能处理“没有检索到资料”的情况。

## 第四阶段：工具调用和 Function Calling

目标：让 Agent 不只是聊天，还能调用工具完成任务。

需要掌握：

- 什么是 tool / function calling。
- 工具 schema 如何定义。
- 模型如何决定调用哪个工具。
- 工具调用结果如何返回给模型。
- 工具参数校验和错误处理。
- 工具权限边界，避免危险操作。

练习工具：

- 当前时间工具。
- 本地文件搜索工具。
- 计算器工具。
- 天气查询工具。
- 数据库查询工具。

练习项目：

- 做一个能查文件、总结文件、回答问题的本地助手。
- 做一个能调用多个工具完成任务的命令行 Agent。

验收标准：

- 能写出 3 个以上稳定工具。
- 能处理工具调用失败、参数错误、结果为空。
- 知道哪些工具需要用户确认，哪些可以自动执行。

## 第五阶段：LangChain 和 LangGraph

目标：用成熟框架提高开发效率，但不被框架牵着走。

学习顺序：

1. LangChain 基础组件
2. PromptTemplate
3. ChatOpenAI
4. OutputParser
5. Retriever
6. Runnable / Chain
7. Tool
8. AgentExecutor
9. LangGraph 状态机和多步骤工作流

重点理解：

- LangChain 是工程封装，不是魔法。
- Chain 本质是把多个步骤串起来。
- Agent 本质是模型根据上下文选择下一步动作。
- LangGraph 更适合复杂、可控、多节点流程。

练习项目：

- 用 LangChain 重写前面的 PDF 问答项目。
- 用 LangChain 实现带工具调用的 Agent。
- 用 LangGraph 实现“计划、执行、反思”的多节点 Agent。

验收标准：

- 能用 LangChain 快速搭建 RAG 应用。
- 能不用 LangChain 解释背后的原始流程。
- 能判断什么时候用 Chain，什么时候用 Agent，什么时候用 LangGraph。

## 第六阶段：记忆系统

目标：让 Agent 能在长期使用中保留上下文。

需要掌握：

- 短期记忆：当前会话历史。
- 长期记忆：用户偏好、历史任务、重要事实。
- 摘要记忆：压缩多轮对话。
- 向量记忆：按语义召回历史内容。
- 记忆更新策略：什么时候写入，什么时候覆盖。
- 隐私和安全：哪些内容不应该长期保存。

练习项目：

- 给聊天机器人增加 JSONL 历史记录。
- 给聊天机器人增加压缩长期摘要。
- 给 Agent 增加“用户偏好记忆”。

验收标准：

- 能区分短期记忆和长期记忆。
- 能避免把所有历史无脑塞进 prompt。
- 能设计一个简单但可用的记忆结构。

## 第七阶段：评估和调试

目标：不靠感觉判断 Agent 好坏，而是有基本评估方法。

需要掌握：

- 日志记录：输入、检索结果、工具调用、模型输出。
- RAG 评估：召回是否正确、回答是否基于资料。
- Agent 评估：任务完成率、工具调用正确率、失败原因。
- Prompt 版本管理。
- 测试集构建。
- 回归测试。

练习项目：

- 给 RAG 项目准备 20 个固定问题。
- 每次改 prompt 后跑一遍评估。
- 记录错误案例并分类：检索错、prompt 错、工具错、模型能力不足。

验收标准：

- 能解释一个 Agent 为什么答错。
- 能通过日志定位问题发生在哪个步骤。
- 能建立最小测试集防止改坏。

## 第八阶段：部署和工程化

目标：把本地脚本变成别人能使用的应用。

需要掌握：

- FastAPI 基础。
- REST API 设计。
- 环境变量和配置管理。
- Docker 基础。
- 数据库基础。
- 异步任务和队列基础。
- 错误处理、超时、重试。
- API 成本控制。

练习项目：

- 把命令行 Agent 改成 FastAPI 服务。
- 增加一个简单前端页面。
- 用 Docker 启动应用和向量数据库。
- 增加日志和错误返回。

验收标准：

- 能让别人通过接口调用你的 Agent。
- 能部署一个最小可用版本。
- 能处理超时、缺 key、数据库不可用等常见问题。

## 第九阶段：综合项目

至少完成 3 个项目，每个项目都要能运行、能演示、能说明架构。

项目 1：个人知识库问答

- 支持导入 Markdown / PDF。
- 支持向量检索。
- 回答带引用。
- 支持多轮追问。

项目 2：工具型 Agent

- 能调用 3 到 5 个工具。
- 能根据用户任务选择工具。
- 有工具错误处理。
- 有执行日志。

项目 3：任务工作流 Agent

- 使用 LangGraph 或自定义状态机。
- 包含计划、执行、检查、总结。
- 能处理中间失败。
- 能输出结构化结果。

加分项目：

- 浏览器自动化 Agent。
- 数据分析 Agent。
- 客服知识库 Agent。
- 代码仓库问答 Agent。

## 推荐学习顺序

第 1 月：

- Python 工程基础。
- OpenAI API 基础。
- 命令行聊天机器人。

第 2 月：

- Embedding。
- 向量数据库。
- RAG 项目。

第 3 月：

- 工具调用。
- LangChain。
- 简单 Agent。

第 4 月：

- LangGraph。
- 记忆系统。
- 评估和日志。

第 5 到 6 月：

- 综合项目。
- FastAPI 部署。
- Docker。
- 简历项目整理。

## 每周学习节奏

周一到周三：

- 学一个新概念。
- 写最小 demo。
- 记录问题。

周四到周五：

- 把 demo 接入项目。
- 做错误处理。
- 整理代码结构。

周末：

- 做一次完整功能整合。
- 写 README。
- 总结本周踩坑和下一步计划。

## 合格 Agent 开发工程师能力清单

达到以下标准，可以认为已经具备初级到合格的 Agent 开发能力：

- 能直接使用 OpenAI API 写聊天应用。
- 能独立实现 RAG 流程。
- 能使用 LangChain 搭建 chain 和 retriever。
- 能写工具调用 Agent。
- 能设计基础记忆系统。
- 能读懂日志并定位问题。
- 能给 Agent 写测试集和评估脚本。
- 能用 FastAPI 把 Agent 封装成服务。
- 能用 Docker 跑起依赖组件。
- 能清楚说明系统架构和技术取舍。

## 不建议一开始做的事

- 一开始就自己写完整框架。
- 一开始就追复杂多 Agent 系统。
- 不理解 OpenAI API 就直接套 LangChain。
- 不做日志和评估，只凭感觉改 prompt。
- 只看教程不做项目。
- 频繁换框架，不沉下去做完整闭环。

## 最小可行学习项目

如果只选一个项目，就做这个：

个人读书助手。

功能范围：

- 导入一本 PDF 或 Markdown。
- 自动切片。
- 生成 embedding。
- 存入向量数据库。
- 用户提问时检索相关片段。
- 模型基于片段回答。
- 保存对话历史。
- 回答附引用。

这个项目能覆盖 Agent 工程师最核心的能力：模型调用、RAG、记忆、工具化、工程结构和调试。

## 最终目标

不是成为“会调包的人”，而是成为能把 AI Agent 从想法落地到可运行系统的人。

你需要同时具备三种能力：

- 原理能力：知道模型、RAG、工具调用、记忆背后怎么工作。
- 工程能力：能写稳定代码、处理错误、部署服务。
- 产品能力：能判断用户真正需要什么样的 Agent，而不是堆功能。
